# redact-pii
A Python + Flask system that uses Azure OCR and Azure OpenAI to detect and redact PII from images. It extracts text, identifies sensitive data like names and emails, and automatically masks it, providing a secure, scalable, cloud-based redaction workflow.
